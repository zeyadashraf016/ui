{
  "name": "eliara-ui",
  "version": "1.0.0",
  "private": true,
  "description": "Eliara Inventory Intelligence — chat UI (deploys to Vercel)",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve dist"
  }
}
